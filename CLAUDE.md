@AGENTS.md

# 项目规则

- 本项目所有交互、回复、注释统一使用中文。代码命名（变量名、函数名、组件名）使用英文。
Git push的动作不要自动做，需要提示我，待我确认后再执行。

1. 上下文token超过60%，主动执行/compact压缩；
2. 禁止一次性读取整个目录全部源码，只读取当前任务相关文件；
3. 读取大文件时优先读取指定行号，不读取完整大文件；
4. shell命令禁止输出完整长日志，只输出关键报错摘要；
5. 完成一个子模块任务，主动输出任务总结，用于会话交接。